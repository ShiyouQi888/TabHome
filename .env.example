# TabHome 环境配置示例文件
# 复制此文件并重命名为 .env.local 来进行本地配置

# =============================================================================
# 数据库配置（必需）
# =============================================================================

# Supabase 项目 URL
# 获取方式：登录 Supabase 控制台 -> 项目设置 -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase 匿名密钥
# 获取方式：登录 Supabase 控制台 -> 项目设置 -> API -> anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase 服务角色密钥（用于服务端操作）
# 获取方式：登录 Supabase 控制台 -> 项目设置 -> API -> service_role
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# 天气API配置（可选）
# =============================================================================

# OpenWeatherMap API 密钥
# 获取方式：注册 https://openweathermap.org/api 获取免费 API 密钥
NEXT_PUBLIC_WEATHER_API_KEY=your-weather-api-key
NEXT_PUBLIC_WEATHER_API_URL=https://api.openweathermap.org/data/2.5

# =============================================================================
# 搜索引擎配置（可选）
# =============================================================================

# 自定义搜索引擎 API 密钥
# 可用于集成 Google Custom Search 或其他搜索引擎
NEXT_PUBLIC_SEARCH_ENGINE_API_KEY=your-search-engine-key

# =============================================================================
# 应用配置
# =============================================================================

# 应用名称
NEXT_PUBLIC_APP_NAME=TabHome

# 应用 URL（本地开发）
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 支持邮箱
NEXT_PUBLIC_SUPPORT_EMAIL=blacklaw@foxmail.com

# =============================================================================
# 开发配置
# =============================================================================

# 开发模式
NODE_ENV=development

# 调试模式（可选）
# DEBUG=tabhome:*

# =============================================================================
# 高级配置（可选）
# =============================================================================

# 会话密钥（用于加密用户会话）
# 生成方式：openssl rand -base64 32
SESSION_SECRET=your-session-secret-here

# JWT 密钥（用于令牌签名）
# 生成方式：openssl rand -base64 64
JWT_SECRET=your-jwt-secret-here

# =============================================================================
# 第三方集成配置（可选）
# =============================================================================

# Google Analytics 跟踪 ID
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Sentry 错误监控（可选）
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn.ingest.sentry.io

# =============================================================================
# 部署配置
# =============================================================================

# Vercel 部署配置（自动设置）
# VERCEL_URL=your-vercel-url.vercel.app
# VERCEL_ENV=production
# VERCEL_GIT_COMMIT_SHA=your-commit-sha
# VERCEL_GIT_COMMIT_MESSAGE=your-commit-message
# VERCEL_GIT_COMMIT_AUTHOR_LOGIN=your-github-username
# VERCEL_GIT_COMMIT_AUTHOR_NAME=your-name

# =============================================================================
# 安全配置
# =============================================================================

# CORS 配置（可选）
CORS_ORIGIN=http://localhost:3000,https://your-domain.com

# 速率限制配置（可选）
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# 获取 API 密钥的指南
# =============================================================================

# Supabase:
# 1. 访问 https://supabase.com/
# 2. 创建新项目或选择现有项目
# 3. 进入项目设置 -> API
# 4. 复制所需的 URL 和密钥

# OpenWeatherMap:
# 1. 访问 https://openweathermap.org/api
# 2. 注册免费账户
# 3. 验证邮箱后获取 API 密钥
# 4. 免费套餐：1000 次调用/天

# Google Analytics:
# 1. 访问 https://analytics.google.com/
# 2. 创建新的分析属性
# 3. 获取跟踪 ID (格式: G-XXXXXXXXXX)

# Sentry:
# 1. 访问 https://sentry.io/
# 2. 创建新项目
# 3. 在项目设置中获取 DSN